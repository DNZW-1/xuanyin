# HTML — 页面结构

游戏入口和页面骨架。所有 .html 文件放这里。

## 文件

| 文件 | 说明 |
|------|------|
| index.html | 主入口，加载 CSS/JS，渲染游戏界面 |

内容包含：DOM 结构、舞台元素、对话框容器、地图 SVG、控制栏。

## 约定

- 语义化标签（main/section/nav）
- 内联事件绑定放在 HTML 属性中引用全局函数
- 复杂交互逻辑写在 JavaScript/ 下
