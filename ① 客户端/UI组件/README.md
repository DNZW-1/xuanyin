# UI组件 — 可复用界面元素

通用的 UI 组件，可在多个界面中重用。

## 规划

| 组件 | 说明 |
|------|------|
| Button.js/css | 按钮（常规/强调/禁用） |
| Modal.js/css | 弹窗/模态框 |
| ProgressBar.js/css | 进度条（修为/好感/血量） |
| Tooltip.js/css | 悬浮提示 |
| TabPanel.js/css | 标签页切换 |

## 约定

- 每个组件一个目录，包含 .js 和 .css
- 组件独立，不依赖其他组件
- 通过配置项自定义样式和行为
