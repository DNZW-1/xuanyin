# 狂澜仙欲录：玄阴 — 项目协议

## 项目身份
NSFW 修仙世界观，暂定名《狂澜仙欲录：玄阴》。玄阴山全女宗门，炉鼎/双修体系为核心张力。
初始三人：主角苏尘、师尊冷凝霜、一个场景"玄阴山夜袭"。

## 核心架构哲学（来自前任 vault 教训）

前 vault（炉鼎之欲）：557 文件，52% 为空 stub，287 孤岛节点，252 坏链。
教训——**没有内容的文件不建，写了内容再建文件。**

架构信条：
1. 每个文件必须有实质内容（>15 行正文），禁止 stub
2. 扁平优先，最多 3 层目录
3. 每个文件最少 1-2 个 `[[外链]]` 加入图谱
4. 不建索引文件，不建"占位"
5. 词条用扁平 glossary/ 目录，不拆几十个子类
6. 模板/系统文件放规则/ 目录，标记图谱排除

## 存档协议（防呆设计）

存档是唯一的游戏真相源，不依赖 AI 上下文记忆。

### 启动时
1. 读 `save.json` 的 `_protocol` 和 `diary[-10:]` 注入上下文
2. 若无 `save.json`，视作新游戏，问用户是否初始化

### 对话中
- 不主动读写存档
- 仅用 diary 摘要维持叙事连贯
- 用户说"存盘""存档""不玩了""继续"等触发读写

### 结束时
1. 追加日记条目到 `diary[]`
2. 更新 `relations`（好感/亲密度变化）
3. 更新 `flags`（已解锁内容）
4. 更新 `player`（境界/状态）
5. 写回 `save.json`

### 约束
- 存档即唯一真相源
- 不把游戏记忆留在 AI 上下文外
- 任何新对话都从 save.json 重建认知

## 文件命名规则
- `角色-名称.md` — 人物
- `场景-名称.md` — 剧情场景
- `设定-名称.md` — 世界观/词条
- `存档-名称.json` — 状态文件
- 名称本身不重复（不拆分"丹药/筑基丹"等几十个 stub）

## NSFW 风格引用
完整风格指南在用户 RAG 库 `nsfw-style-guides`。
查询命令：`python obsidian_rag.py query nsfw-style-guides <问题>`
核心规则摘要见下方。

## 用户工具链
- Obsidian 库路径：E:\Obsidian\（多个分库）
- 随笔库：E:\Obsidian\随笔
- obsidian-rag 脚本：D:\codex_Chat_record\obsidian_rag.py
- 嵌入引擎：Ollama bge-m3（维度 1024）
- 向量存储：.rag_data/（numpy + JSON）

## 操守

以瞎猜糊弄为耻，以认真有效为荣 — 不确定时不编造答案，先问用户或查源码。

以臆想流程为耻，以可以复现为荣 — 每个操作步骤必须记录准确命令和参数，确保他人可复现。

以破坏架构为耻，以遵循规范为荣 — 增删改遵循项目既有模式（文件命名、层级、存档协议等）。

以假装理解为耻，以诚实无知为荣 — 不理解用户意图或代码时直接承认，追问澄清，不假装明白。

以盲目修改为耻，以谨慎重构为荣 — 改代码前先读上下文（相关文件），评估影响范围后再动手。

### 操守执行细则
1. 产生新内容前，先确认文件已存在且有实质内容，禁止创建 stub
2. 改代码前，先用 Read 读文件，用 Grep 搜索相关引用
3. 每一步操作后，可用命令验证结果（git status, 编译/运行测试等）
4. 需要用户决策时，明确列出选项和理由，不做假设
5. 涉及 token/密码等敏感信息，用后立即从可追溯记录中清理

## 上传流程（已保存教训）
见 `.opencode/lessons.md`

## 扩展指南
